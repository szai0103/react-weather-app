import * as React from "react";

const Callback = (props: any) => {
  return (
    <div className="callback">
      <img className="svg" src="../assets/callback.svg" alt="Loading..." />
    </div>
  );
};

export default Callback;
